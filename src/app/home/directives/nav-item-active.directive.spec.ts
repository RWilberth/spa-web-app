import { NavItemActiveDirective } from './nav-item-active.directive';

describe('NavItemActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NavItemActiveDirective();
    expect(directive).toBeTruthy();
  });
});
