import { FackeAuthBackendInterceptor } from './facke-auth-backend-interceptor';

describe('FackeAuthBackendInterceptor', () => {
  it('should create an instance', () => {
    expect(new FackeAuthBackendInterceptor()).toBeTruthy();
  });
});
